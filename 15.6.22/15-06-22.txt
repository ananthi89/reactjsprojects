step1:
Install the packages:
cmd:npm install redux
cmd:npm install react-redux 


step2:
create the store inside the index.js
 
import { createStore } from 'redux';
import { Provider } from 'react-redux';
import rootReducers from './reducers';
const store = createStore(rootReducers);


root.render(
  <React.StrictMode>
    <Provider store={store}>
      <App />
    </Provider>
  </React.StrictMode>
);


step3:
please create the reducers folder and create two file index.js and user.js
index.js will combine all reducers together
user.js will be local state for login component 
//for each component you can have differe state file.


index.js 

import { combineReducers } from "redux";
import users from './users';

const rootReducers = combineReducers({
     users:users
})

export default rootReducers;



user.js 

const initialState = {
    status:false,
    fullName:'',
    list:[]
};

function users(state = initialState, action){

 switch(action.type){
     case 'success' : 
               return {
                   ...state,
                   list:action.payload,
                   fullName:"salman",
                   status:true
               }
   case 'failure':
       return{
           ...state,
           fullName:"noname",
           status:false
       }
       
       default:          
       return state;   
 }

}

export default users;



step 4 :
creating dispatch
create one folder called service and create one dispatch file 


const Users = {
    
    loadUsers(dispatch) {
        dispatch({
            type: 'success',
            payload: [44,55,66,55]
        })
      
    }
}
export default Users;

